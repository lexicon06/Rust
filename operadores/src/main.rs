fn main() {
    let numero_uno: i32 = 10;
    let numero_dos: i32 = 200;

    let resultado: i32 = numero_uno + numero_dos; //basic math + - / *

    let res_bool: bool = resultado > 100; // > < != == && ||



    println!("{}, {}", resultado, res_bool);
}
